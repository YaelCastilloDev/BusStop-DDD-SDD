# Start-SonarQube.ps1 creates .env with a random database password automatically.
# For manual Compose setup, copy this file to .env and replace the placeholder.
# Keep the same password when restarting an existing database volume.
SONAR_DB_PASSWORD=replace-with-a-long-random-password
